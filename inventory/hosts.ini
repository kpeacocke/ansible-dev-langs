# Local control node target. Add remote hosts below as needed.
[all]
localhost ansible_connection=local

# Example remote Linux hosts (uncomment and edit):
# [linux]
# workstation1.example.com ansible_user=devuser

# Example remote Windows hosts (uncomment and edit):
# [windows]
# win-workstation.example.com
#
# [windows:vars]
# ansible_connection=winrm
# ansible_winrm_transport=ntlm
# ansible_port=5986
# ansible_winrm_server_cert_validation=ignore
